<h1>My tasks</h1>

<ul>
  <% @tasks.each do |t| %>
   <li> 
    <input type="checkbox" <%= t.completed ? "checked" : "" %> >
    <%= link_to t.title, task_path(t.id)%>
    <%= t.completed %>
   </li>
  <% end %>
</ul>
